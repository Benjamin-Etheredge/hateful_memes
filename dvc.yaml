stages:
  test_visual_bert_with_od:
    cmd: >
        python -m hateful_memes.models.visual_bert_with_od
        --batch_size 4 
        --fast_dev_run True
        --log_dir logs/test_visual_bert_with_od
        --project test
        && echo "PASSED" > logs/test_visual_bert_with_od.log
    deps:
      - data/01_raw/hateful_memes
      - hateful_memes/models/visual_bert_with_od.py
    outs:
      - logs/test_visual_bert_with_od.log

  test_beit:
    cmd: >
        python -m hateful_memes.models.baseIT
        --model_name vit
        --batch_size 4 
        --fast_dev_run True
        --log_dir logs/test_simple_beit_image_metrics
        --project test
        && echo "PASSED" > logs/test_beit.log
    deps:
      - data/01_raw/hateful_memes
      - hateful_memes/models/baseIT.py
    outs:
      - logs/test_beit.log

  beit:
    vars:
      - config/beit.yaml
    cmd: >
        python -m hateful_memes.models.baseIT
        --model_name ${model_name}
        --batch_size ${batch_size}
        --lr ${lr}
        --epochs ${epochs}
        --max_length ${max_length}
        --dense_dim ${dense_dim}
        --dropout_rate ${dropout_rate}
        --grad_clip ${gradient_clip_value}
        --model_dir data/06_models/beit
    deps:
      - logs/test_beit.log
      - data/01_raw/hateful_memes
      - hateful_memes/models/baseIT.py
    outs:
      - data/06_models/beit

  test_vit:
    cmd: >
        python -m hateful_memes.models.baseIT
        --model_name vit
        --batch_size 4 
        --fast_dev_run True
        --log_dir logs/test_simple_vit_image_metrics
        --project test
        && echo "PASSED" > logs/test_vit.log
    deps:
      - data/01_raw/hateful_memes
      - hateful_memes/models/baseIT.py
    outs:
      - logs/test_vit.log

  vit:
    vars:
      - config/vit.yaml
    cmd: >
        python -m hateful_memes.models.baseIT
        --model_name ${model_name}
        --batch_size ${batch_size}
        --lr ${lr}
        --epochs ${epochs}
        --max_length ${max_length}
        --dense_dim ${dense_dim}
        --dropout_rate ${dropout_rate}
        --grad_clip ${grad_clip}
        --model_dir data/06_models/vit
    deps:
      - logs/test_vit.log
      - data/01_raw/hateful_memes
      - hateful_memes/models/baseIT.py
    outs:
      - data/06_models/vit

  test_visual_bert:
    cmd: >
        python -m hateful_memes.models.visual_bert
        --batch_size 4 
        --fast_dev_run True
        --log_dir logs/test_simple_visual_bert_image_metrics
        --project test
        && echo "PASSED" > logs/test_visual_bert.log 
    deps:
      - data/01_raw/hateful_memes
      - hateful_memes/models/base.py
      - hateful_memes/models/visual_bert.py
    outs:
      - logs/test_visual_bert.log

  visual_bert:
    vars:
      - config/visual_bert.yaml
    cmd: >
        python -m hateful_memes.models.visual_bert
        --batch_size ${batch_size}
        --lr ${lr}
        --epochs ${epochs}
        --max_length ${max_length}
        --dense_dim ${dense_dim}
        --dropout_rate ${dropout_rate}
        --grad_clip ${gradient_clip_value}
        --model_dir data/06_models/visual_bert
    deps:
      - logs/test_visual_bert.log
      - data/01_raw/hateful_memes
      - hateful_memes/models/base.py
      - hateful_memes/models/visual_bert.py
    outs:
      - data/06_models/visual_bert

  test_electra:
    vars:
      - config/electra.yaml
    cmd: >
        python -m hateful_memes.models.auto_text_model
        --batch_size 4 
        --fast_dev_run True
        --model_name ${model_name}
        && echo "PASSED" > logs/test_electra.log 
    deps:
      - data/01_raw/hateful_memes
      - hateful_memes/models/auto_text_model.py
    outs:
      - logs/test_electra.log

  electra:
    vars:
      - config/electra.yaml
    cmd: >
        python -m hateful_memes.models.auto_text_model
        --batch_size ${batch_size}
        --lr ${lr}
        --epochs ${epochs}
        --max_length ${max_length}
        --dense_dim ${dense_dim}
        --dropout_rate ${dropout_rate}
        --grad_clip ${gradient_clip_value}
        --model_name ${model_name}
        --project electra
        --model_dir data/06_models/electra
    deps:
      - logs/test_electra.log
      - data/01_raw/hateful_memes
      - hateful_memes/models/auto_text_model.py
    outs:
      - data/06_models/electra

  test_distilbert:
    vars:
      - config/distilbert.yaml
    cmd: >
        python -m hateful_memes.models.auto_text_model
        --batch_size 4 
        --fast_dev_run True
        --model_name ${model_name}
        && echo "PASSED" > logs/test_distilbert.log 
    deps:
      - data/01_raw/hateful_memes
      - hateful_memes/models/auto_text_model.py
    outs:
      - logs/test_distilbert.log

  distilbert:
    vars:
      - config/distilbert.yaml
    cmd: >
        python -m hateful_memes.models.auto_text_model
        --batch_size ${batch_size}
        --lr ${lr}
        --epochs ${epochs}
        --max_length ${max_length}
        --dense_dim ${dense_dim}
        --dropout_rate ${dropout_rate}
        --grad_clip ${gradient_clip_value}
        --model_name ${model_name}
        --project distilbert
        --model_dir data/06_models/distilbert
    deps:
      - logs/test_distilbert.log
      - data/01_raw/hateful_memes
      - hateful_memes/models/auto_text_model.py
    outs:
      - data/06_models/distilbert

  test_simple_mlp_image:
    cmd: >
        python -m hateful_memes.models.simple_mlp_image
        --fast_dev_run True
        --log_dir logs/test_simple_mlp_image_metrics
        --project test
        && echo "PASSED" > logs/test_simple_mlp_image.log 
    deps:
      - data/01_raw/hateful_memes
      - hateful_memes/models/base.py
      - hateful_memes/models/simple_mlp_image.py
    outs:
      - logs/test_simple_mlp_image.log
        
  simple_mlp_image:
    vars:
      - config/simple_mlp_image.yaml
    cmd: >
        python -m hateful_memes.models.simple_mlp_image
        --batch_size ${batch_size}
        --lr ${lr}
        --epochs ${epochs}
        --dense_dim ${dense_dim}
        --dropout_rate ${dropout_rate}
        --grad_clip ${gradient_clip_value}
        --model_dir data/06_models/simple_mlp_image
    deps:
      - logs/test_simple_mlp_image.log
      - data/01_raw/hateful_memes
      - hateful_memes/models/base.py
      - hateful_memes/models/simple_mlp_image.py
    outs:
      - data/06_models/simple_mlp_image

  test_simple_image:
    cmd: >
        python -m hateful_memes.models.simple_image
        --log_dir logs/test_simple_image_metrics
        --project test
        --fast_dev_run True
        && echo "PASSED" > logs/test_simple_image.log
    deps:
      - data/01_raw/hateful_memes
      - hateful_memes/models/base.py
      - hateful_memes/models/simple_image.py
    outs:
      - logs/test_simple_image.log

  simple_image:
    vars: 
      - config/simple_image.yaml
    cmd: >
        python -m hateful_memes.models.simple_image
        --batch_size ${batch_size}
        --lr ${lr}
        --epochs ${epochs}
        --dense_dim ${dense_dim}
        --dropout_rate ${dropout_rate}
        --grad_clip ${gradient_clip_value}
        --model_dir data/06_models/simple_image
        --batch_norm ${batch_norm}
    deps:
      - logs/test_simple_image.log
      - data/01_raw/hateful_memes
      - hateful_memes/models/simple_image.py
      - hateful_memes/models/base.py
    outs:
      - data/06_models/simple_image

  baseline:
    cmd: python -m hateful_memes.models.baseline_algorithm
    deps:
      - data/01_raw/hateful_memes
      - hateful_memes/models/baseline_algorithm.py

  test_simple_text:
    cmd: >
        python -m hateful_memes.models.simple_text
        --fast_dev_run True
        && echo "PASSED" > logs/test_simple_text.log
    deps:
      - data/01_raw/hateful_memes
      - hateful_memes/models/base.py
      - hateful_memes/models/simple_text.py
    outs:
      - logs/test_simple_text.log

  simple_text:
    vars: 
      - config/simple_text.yaml
    cmd: >
        python -m hateful_memes.models.simple_text
        --batch_size ${batch_size}
        --lr ${lr}
        --num_layers ${num_layers}
        --embed_dim ${embed_dim}
        --dense_dim ${dense_dim}
        --max_length ${max_length}
        --tokenizer_name ${tokenizer_name}
        --grad_clip ${grad_clip}
        --dropout_rate ${dropout_rate}
        --epochs ${epochs}
        --model_dir data/06_models/simple_text
    deps:
      - data/01_raw/hateful_memes
      - logs/test_simple_text.log
      - hateful_memes/models/base.py
      - hateful_memes/models/simple_text.py
    outs:
      - data/06_models/simple_text

  test_super_model:
    cmd: >
        python -m hateful_memes.models.super_model
        --simple_image_chkpt data/06_models/simple_image
        --simple_text_chkpt data/06_models/simple_text
        --fast_dev_run True
        && echo "PASSED" > logs/test_super_model.log
    deps:
      - data/01_raw/hateful_memes
      - hateful_memes/models/base.py
      - hateful_memes/models/super_model.py
      - data/06_models/simple_image
      - data/06_models/simple_text
    outs:
      - logs/test_super_model.log

  super_model:
    vars: 
      - config/super_model.yaml
    cmd: >
        python -m hateful_memes.models.super_model
        --simple_image_chkpt data/06_models/simple_image
        --simple_mlp_image_chkpt data/06_models/simple_mlp_image
        --simple_text_chkpt data/06_models/simple_text
        --visual_bert_chkpt data/06_models/visual_bert
        --vit_chkpt data/06_models/vit
        --beit_chkpt data/06_models/beit
        --batch_size ${batch_size}
        --lr ${lr}
        --num_dense_layers ${num_layers}
        --dense_dim ${dense_dim}
        --dropout_rate ${dropout_rate}
        --epochs ${epochs}
        --model_dir data/06_models/super_model
    deps:
      - data/01_raw/hateful_memes
      - logs/test_super_model.log
      - hateful_memes/models/base.py
      - hateful_memes/models/super_model.py
      - data/06_models/simple_image
      - data/06_models/simple_mlp_image
      - data/06_models/simple_text
      - data/06_models/visual_bert
      - data/06_models/vit
      - data/06_models/beit
    outs:
      - data/06_models/super_model
  
  hateful_memes_to_snli_ve:
    cmd: python hateful_memes/data/hateful_memes_snli_ve.py
    deps: 
      - data/01_raw/hateful_memes
      - hateful_memes/data/hateful_memes_snli_ve.py
    outs:
      - data/02_intermediate/hateful_memes_snli_ve.tsv
 
  test_hateful_ofa:
    vars:
      - config/hateful_ofa.yaml
    cmd: >
      python -m hateful_memes.models.hateful_ofa
      ${data}
      --fast_dev_run
      --selected-cols ${selected-cols}
      --bpe-dir ${bpe-dir}
      --user-dir ${user-dir}
      --restore-file ${restore-file}
      --save-dir {save-dir}
      --task {task} 
      --arch ${arch}
      --label-smoothing ${label-smoothing}
      --batch-size ${batch-size}
      --encoder-normalize-before
      --decoder-normalize-before
      --share-decoder-input-output-embed
      --share-all-embeddings
      --layernorm-embedding 
      --patch-layernorm-embedding
      --code-layernorm-embedding 
      --resnet-drop-path-rate ${resnet-drop-path-rate} 
      --encoder-drop-path-rate ${encoder-drop-path-rate}
      --decoder-drop-path-rate ${decoder-drop-path-rate}
      --dropout ${dropout} 
      --attention-dropout ${attention-dropout}
      --weight-decay ${weight-decay} 
      --optimizer ${optimizer} 
      --adam-betas ${adam-betas}
      --adam-eps ${adam-eps}
      --clip-norm ${clip-norm}
      --lr ${lr} 
      --max-epoch ${max-epoch} 
      --fixed-validation-seed ${fixed-validation-seed} 
      --max-src-length ${max-src-length}
      --max-tgt-length ${max-tgt-length}
      --find-unused-parameters 
      --add-type-embedding 
      --scale-attn 
      --scale-fc 
      --scale-heads 
      --disable-entangle 
      --num-bins ${num-bins}
      --patch-image-size ${patch-image-size}
      --prompt-type ${prompt-type} 
      --add-caption 
      --fp16 
      --fp16-scale-window ${fp16-scale-window}
      --ema-alpha ${ema-alpha} 
      --num-workers ${num-workers}
      && echo "PASSED" > logs/test_hateful_ofa.log
    deps:
      - data/02_intermediate/hateful_memes_snli_ve.tsv
      - hateful_memes/models/hateful_ofa.py
    outs:
      - logs/test_hateful_ofa.log